name: Kontrol
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'  

jobs:
  BirlesikGuncelleme:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - name: Depoyu Klonla
        uses: actions/checkout@v4.2.2
        
      - name: Python 3.11.8 Yükle
        uses: actions/setup-python@v5.3.0
        with:
          python-version: 3.11.8
          
      - name: Gereksinimleri Kur
        run: |
          python -m pip install --upgrade pip
          pip install -U setuptools wheel requests beautifulsoup4
          
      - name: kbl.py Çalıştır
        continue-on-error: true
        run: python kbl.py
        
      - name: selcuk.py Çalıştır
        continue-on-error: true
        run: python selcuk.py    
        
      - name: Değişiklik Kontrolü ve Sahneleme
        id: degisiklik_kontrol
        run: |
          git add -A  # Yeni oluşturulan klasörleri (selcuk/kablo) git'e tanıt
          # Değişiklik olup olmadığını kontrol et
          if [ -n "$(git status --porcelain)" ]; then
            echo "degisiklik=true" >> $GITHUB_OUTPUT
            echo "✅ Değişiklik tespit edildi."
          else
            echo "degisiklik=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Herhangi bir değişiklik yok."
          fi
          
      - name: Değişiklikleri Commit Et
        if: steps.degisiklik_kontrol.outputs.degisiklik == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "🔄 Kanallar güncellendi" \
                     -m "📺 Selcuk ve Kablo M3U8 dosyaları otomatik güncellendi" \
                     -m "🕒 İşlem Tarihi: $(date)" \
                     -m "SHA: $GITHUB_SHA"
          git push
