name: Kontrol
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'  

jobs:
  BirlesikGuncelleme:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - name: Depoyu Klonla
        uses: actions/checkout@v4.2.2
        
      - name: Python 3.11.8 Yükle
        uses: actions/setup-python@v5.3.0
        with:
          python-version: 3.11.8
          
      - name: Gereksinimleri Kur
        run: |
          python -m pip install --upgrade pip
          pip install -U setuptools wheel requests beautifulsoup4
          
      # Görüntüdeki tüm scriptlerin çalıştırılması
      - name: Scriptleri Çalıştır
        continue-on-error: true
        run: |
          python kbl.py
          python selcuk.py
          python ftb.py
          python int.py
          python jst.py
          python liveeventsfilter.py
          python oki.py
          python r.py
          python r2.py
          python ml.py
          python sc.py
          # Eğer yeni paylaştığım Andro (Atom) scriptini eklediysen:
          # python an.py 

      # En son tüm dosyaları ve klasörleri birleştiren adım
      - name: Kanalları Birleştir (Merger)
        continue-on-error: true
        run: python merger.py # Dosya adın farklıysa burayı düzelt (örn: birlestirici.py)
        
      - name: Değişiklik Kontrolü ve Sahneleme
        id: degisiklik_kontrol
        run: |
          # Önemli: Yeni oluşturulan tüm klasörleri git'e ekler
          git add -A 
          if [ -n "$(git status --porcelain)" ]; then
            echo "degisiklik=true" >> $GITHUB_OUTPUT
            echo "✅ Değişiklik tespit edildi."
          else
            echo "degisiklik=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Herhangi bir değişiklik yok."
          fi
          
      - name: Değişiklikleri Commit Et
        if: steps.degisiklik_kontrol.outputs.degisiklik == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "🔄 Kanallar güncellendi" \
                     -m "📺 Tüm kaynaklar (Inat, Jest, Selcuk, Kablo vb.) otomatik güncellendi" \
                     -m "🕒 İşlem Tarihi: $(date)"
          git push
