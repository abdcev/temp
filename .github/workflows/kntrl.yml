name: Kontrol
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'  

jobs:
  BirlesikGuncelleme:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - name: Depoyu Klonla
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0 # Tüm geçmişi çek (pull/rebase için gerekli)
        
      - name: Python 3.11.8 Yükle
        uses: actions/setup-python@v5.3.0
        with:
          python-version: 3.11.8
          
      - name: Gereksinimleri Kur
        run: |
          python -m pip install --upgrade pip
          pip install -U setuptools wheel requests beautifulsoup4 cloudscraper
          
      - name: Scriptleri Çalıştır
        continue-on-error: true
        run: |
          python kbl.py
          python selcuk.py

      - name: Kanalları Birleştir (Merger)
        continue-on-error: true
        run: python merger.py
        
      - name: Değişiklikleri İşle ve Push Et
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # Yeni dosyaları ekle
          git add -A
          
          # Değişiklik var mı kontrol et
          if [ -n "$(git status --porcelain)" ]; then
            echo "✅ Değişiklikler commit ediliyor..."
            git commit -m "🔄 Kanallar güncellendi - $(date)"
            
            # KRİTİK NOKTA: Uzaktaki değişiklikleri çek ve kendi commitini en üste koy
            git pull --rebase origin main
            
            # Son hali pushla
            git push origin main
          else
            echo "ℹ️ Herhangi bir değişiklik yok, işlem atlanıyor."
          fi
